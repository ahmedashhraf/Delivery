package com.instabug.library.tracking;

public enum ActivityLifeCycleEvent {
    CREATED,
    STARTED,
    RESUMED,
    PAUSED,
    STOPPED,
    DESTROYED;

    static {
        boolean[] $jacocoInit;
        $jacocoInit[3] = true;
    }
}
